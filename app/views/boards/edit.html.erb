<h1>スレッド編集</h1>

<% if @board.user_id == current_user.id %>
  <%= render 'form', board: @board %>
<% end %>

<% if @board.user_id != current_user.id %>
  <p class="alert">スレッドの編集権限がありません。</p>
<% end %>



<%= link_to '詳細', @board %>
<%= link_to '一覧に戻る', boards_path %>
